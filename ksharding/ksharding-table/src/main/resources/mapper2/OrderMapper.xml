<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.beckbi.dao.OrderMapper">
    <select id="getOrdersByUid" parameterType="java.lang.Long"  resultType="cn.beckbi.model.Order">
        select * from `order` where uid = #{uid}
    </select>
</mapper>